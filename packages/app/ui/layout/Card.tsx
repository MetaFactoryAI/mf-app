import { styled } from 'nativewind';
import { View } from 'react-native';
import { RETRO_FRAME_CLASSES } from 'app/ui/theme/constants';

export const Card = styled(View, `bg-gray-9 ${RETRO_FRAME_CLASSES} `, {
  variants: {},
  defaultProps: {},
});
