table:
  name: timezones_enum
  schema: omni
is_enum: true
array_relationships:
- name: users
  using:
    foreign_key_constraint_on:
      column: timezone
      table:
        name: users
        schema: omni
