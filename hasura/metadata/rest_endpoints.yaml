- definition:
    query:
      collection_name: allowed-queries
      query_name: GetExistingLockAccessCode
  url: getExistingLockAccessCode/:lockId/:address
  methods:
  - GET
  name: GetExistingLockAccessCode
  comment: Return an existing access code if the customer with the given ETH address
    already generated one for the given lock.
